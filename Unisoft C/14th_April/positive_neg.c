#include<stdio.h>
#include<conio.h>

void main() {
    int a;

    printf("Enter the number: ");
    scanf("%d", &a);

    if(a<0) {
        printf("Number is negative");
    }

    else if {
        printf("Number is positive");
    }

    else {
        printf("It is zero");
    }
    
}